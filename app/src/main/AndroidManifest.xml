<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.skwen.foru">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/main_module_ic_launcher"
        android:label="@string/main_module_app_name"
        android:roundIcon="@mipmap/main_module_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/main_module_AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="label,allowBackup"
        >
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



    </application>

</manifest>