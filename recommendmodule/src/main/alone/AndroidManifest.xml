<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.skwen.foru.recommendmodule">


    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/recommend_module_ic_launcher"
        android:label="@string/recommend_module_app_name"
        android:roundIcon="@mipmap/recommend_module_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/recommend_module_AppTheme"
        tools:replace="label,allowBackup"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".alone.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


    </application>

</manifest>
